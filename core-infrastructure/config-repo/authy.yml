# Authy Service Configuration
# These properties are served by the Config Server to the Authy Service

server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authy
    username: omar
    password: Omaraymanlove8599_
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Security settings managed centrally
identity:
  mfa:
    enabled: true
  jwt:
    expiration: 3600000 # 1 hour
    refresh-expiration: 604800000 # 7 days
