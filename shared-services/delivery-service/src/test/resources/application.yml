spring:
  application:
    name: delivery-service-test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
    eureka:
      enabled: false

bosta:
  api:
    base-url: http://localhost:8080
  api-key: test-key

eureka:
  client:
    enabled: false
